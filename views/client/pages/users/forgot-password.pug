extends ../../layouts/default.pug
block main
    .container
        .login-form-list
            form(
                    action="/user/password/forgot"
                    method="POST"
                    )
                    .form
                        h3
                            .heading #{pageTitle}
                        .form-group
                            input(
                                class="form-control"
                                name="email"
                                id="email"
                                type="text" 
                                placeholder="Email"
                                required
                                )
                            label(for="email")
                                i.fa-solid.fa-envelope
                            span.form-message
                        button.form-submit(type="submit") Gửi mã OTP
                        .tab.display-flex
                            p Bạn đã nhớ mật khẩu?
                            a(href="/user/login") Đăng nhập ngay