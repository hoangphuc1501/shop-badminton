extends ../../layouts/default.pug
block main
    .container
        .login-form-list
            form(
                    action="/user/password/reset"
                    method="POST"
                    )
                    .form
                        h3
                            .heading #{pageTitle}
                        .form-group
                            input(
                                class="form-control"
                                name="password"
                                id="password" 
                                type="password" 
                                placeholder="Nhập mật khẩu"
                                required
                                )
                            label(for="password")
                                i.fa-solid.fa-lock
                            span#errPass1.form-message
                        button.form-submit(type="submit") Đổi mật khẩu